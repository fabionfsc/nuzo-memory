{
  "name": "@nuzo/mcp-server",
  "version": "1.2.0",
  "private": true,
  "description": "MCP server for Nuzo.",
  "keywords": [
    "ai-agents",
    "mcp",
    "memory",
    "codex",
    "claude-code",
    "local-first"
  ],
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=22",
    "npm": ">=10"
  },
  "bin": {
    "nuzo-mcp-server": "dist/index.js",
    "nuzo-memory-hook": "dist/host-hook-cli.js"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "npm run build -w @nuzo/memory-core && tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json",
    "test": "npm run build -w @nuzo/memory-core && npm run build && vitest run src"
  },
  "dependencies": {
    "@nuzo/memory-core": "1.2.0",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
