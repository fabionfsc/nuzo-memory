{
  "name": "@nuzo/memory-cli",
  "version": "0.3.1",
  "private": true,
  "description": "Command line interface for Nuzo.",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=22",
    "npm": ">=10"
  },
  "bin": {
    "nuzo": "dist/index.js"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "npm run build -w @nuzo/memory-core && tsc -p tsconfig.json --noEmit",
    "test": "npm run build -w @nuzo/memory-core && npm run build && vitest run src"
  },
  "dependencies": {
    "@nuzo/memory-core": "0.3.1",
    "commander": "^15.0.0"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
